<%
aaData = [];
@admissions.each {|admission| aaData <<  [
    link_to(admission.admission_no, admission_path(admission)),
    admission.name,
    admission.father_name,
    admission.last_klass.try(:display_name),
    admission.admission_batch.try(:display_name),
    admission.last_institution,
    admission.last_batch_result_in_per,
    "#{link_to('Edit', edit_admission_path(admission))} / #{link_to 'Review',  review_admission_path(admission)}"
                                ]}
%>

<%={:iTotalRecords => @total,
    :iTotalDisplayRecords => @total,
    :aaData => aaData}.to_json.html_safe%>


