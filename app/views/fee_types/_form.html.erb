<div class="inner-box-container">

  <%= simple_form_for(@fee_type) do |f| %>
    <% if @fee_type.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@fee_type.errors.count, "error") %> prohibited this fee from being saved:</h2>

        <ul>
        <% @fee_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

  
    <%= f.input :name %>
    <%= f.input :description %>

    <div class="input">
      <label>&nbsp;</label>
      <div class="actions">
        <%= f.submit %>
        <%= link_to 'Back', documents_path, :class => "link-button" %>
      </div>
    </div>
  
  <% end %>
</div>